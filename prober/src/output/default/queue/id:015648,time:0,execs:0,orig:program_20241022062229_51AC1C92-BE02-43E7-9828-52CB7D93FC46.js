const v1 = Symbol.toPrimitive;
for (let v2 = 0; v2 < 10; v2++) {
    function F3(a5, a6) {
        if (!new.target) { throw 'must be called with new'; }
        this.hasOwnProperty(v1);
    }
    new F3(v1, v1);
}
gc();
