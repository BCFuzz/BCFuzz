function F0(a2, a3) {
    if (!new.target) { throw 'must be called with new'; }
    for (let v4 = 0; v4 < 10; v4++) {
        const v5 = -v4;
        try { BigUint64Array(F0, BigUint64Array, v5); } catch (e) {}
        ("valueOf").slice(BigUint64Array).search();
        for (let v11 = 0; v11 < 250; v11++) {
        }
    }
}
new F0(F0, F0);
gc();
