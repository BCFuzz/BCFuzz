const v0 = `
    async function* f1(a2, a3) {
        f1 instanceof Uint8Array;
        Array();
        try { Uint8Array.fromHex(); } catch (e) {}
        if (!((Uint8Array >>> Uint8Array) > 268435439)) {
        }
        return a3;
    }
    f1(f1, f1);
`;
for (let i16 = 0, i17 = 10; i16 < i17; i17--) {
}
eval(v0).next(eval);
gc();
